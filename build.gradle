// ===== OOP-game/build.gradle (root) =====
subprojects {
  apply plugin: 'java'

  java {
    toolchain { languageVersion = JavaLanguageVersion.of(17) }
  }

  repositories {
    mavenCentral()
  }
}
// (ไม่มี plugins { application } ที่ root)
